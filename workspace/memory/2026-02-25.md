# 2026-02-25

## EDNA Claw
- Replaced chat avatar from default OpenClaw red crab to EDNA interconnected-dots logo (converted favicon.svg → edna-avatar.png)

## Help Genie Claw (claw.helpgenie.ai)
- **SSH access:** Have SSH key to droplet at 170.64.144.218 (key: ~/.ssh/helpgenie_droplet)
- **Fixed crash:** Help Genie Claw put `identity` under `agents.defaults` (invalid). Moved to `ui.assistant.name: "Help Genie Claw"` and restarted
- **Auth mode:** Changed gateway auth from `mode: "token"` to `mode: "password"` with password `HGClawAccess`
- **Login page:** Built custom login gate (`ui/src/ui/views/login.ts` + `login.css`) — simple password prompt shown when not authenticated, replacing the confusing token/overview screen
- **Key lesson:** Sam does NOT want to touch any console/CLI ever. Handle all infra (restarts, deploys, config changes) via SSH ourselves.
- **Updated SSH key** for Help Genie Claw's own sandbox access to droplet (new key: `ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIEFIuvSJuFbtSZCe2+xbHw2BqNxVYg5d469vlog+63YM helpgenie-claw`)
- **Note:** Changes were made directly on the droplet, NOT via git push. Should be committed to the repo to avoid being overwritten by auto-deploy.

## TODO (next session)
- Build a similar password login page for EDNA Claw (our instance) — same clean flow as Help Genie Claw
- The login gate code is in Help Genie Claw's repo as reference: `ui/src/ui/views/login.ts`, `ui/src/styles/login.css`, and the gate logic in `app-render.ts`

## UI Icon Rendering Issue (03:33-03:50 UTC)
- Sam reported left sidebar icons broken/missing in EDNA-OpenClaw UI
- I wasted time on color/SVG attribute fixes when the real issue was lit-html TemplateResult reuse
- Icons were static `html` tagged templates — when same icon used in rail + nav, lit-html steals DOM from first render site
- Fix: changed icons to factory functions behind a Proxy so each access returns fresh TemplateResult
- Commits: f63813c (SVG attrs), f9487b3 (Proxy fix) — but Sam moved on before confirming fix worked
- **Lesson: when icons are MISSING (not wrong color), think about rendering/component lifecycle first, not styling**
- Sam mentioned "Codex 5.3 solved this in 5 seconds" on Help Genie Claw (couldn't find repo — not accessible with current GitHub token)
- Need to verify the Proxy fix actually works on next opportunity
